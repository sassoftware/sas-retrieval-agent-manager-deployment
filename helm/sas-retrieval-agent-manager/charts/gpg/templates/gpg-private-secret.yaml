{{- if .Values.global.configuration.gpg.privateKey -}}
apiVersion: v1
kind: Secret
metadata:
  name: gpg-private-key
  labels:
    {{- include "gpg.labels" . | nindent 4 }}
stringData:
  privateKey: {{ .Values.global.configuration.gpg.privateKey | quote }}
{{- end }}
