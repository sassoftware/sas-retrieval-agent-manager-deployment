apiVersion: v1
kind: ConfigMap
metadata:
  name: plugin-config
  labels:
    {{- include "sas-retrieval-agent-manager-api.labels" . | nindent 4 }}
data:
  plugin_runner_repo: "{{ .Values.global.configuration.plugin.image.repo.base }}/{{ .Values.global.configuration.plugin.image.repo.path }}"
  plugin_runner_tag: {{ .Values.global.configuration.plugin.image.tag }}
