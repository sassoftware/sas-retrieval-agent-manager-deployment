suite: PostgREST HPA tests
templates:
  - templates/db/rest/hpa.yaml
values:
  - ../values.yaml

tests:
  # ============================================================================
  # HPA creation tests
  # ============================================================================
  - it: should not render HPA when autoscaling is disabled
    set:
      db.rest.autoscaling.enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should render HPA when autoscaling is enabled
    set:
      db.rest.autoscaling.enabled: true
      db.rest.autoscaling.minReplicas: 2
      db.rest.autoscaling.maxReplicas: 10
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: HorizontalPodAutoscaler
      - isAPIVersion:
          of: autoscaling/v2

  # ============================================================================
  # HPA configuration tests
  # ============================================================================
  - it: should set minReplicas and maxReplicas
    set:
      db.rest.autoscaling.enabled: true
      db.rest.autoscaling.minReplicas: 3
      db.rest.autoscaling.maxReplicas: 15
    asserts:
      - equal:
          path: spec.minReplicas
          value: 3
      - equal:
          path: spec.maxReplicas
          value: 15

  - it: should target the correct deployment
    set:
      db.rest.autoscaling.enabled: true
    asserts:
      - equal:
          path: spec.scaleTargetRef.apiVersion
          value: apps/v1
      - equal:
          path: spec.scaleTargetRef.kind
          value: Deployment
      - matchRegex:
          path: spec.scaleTargetRef.name
          pattern: ".*-postgrest$"

  # ============================================================================
  # CPU metric tests
  # ============================================================================
  - it: should include CPU metric when targetCPUUtilizationPercentage is set
    set:
      db.rest.autoscaling.enabled: true
      db.rest.autoscaling.targetCPUUtilizationPercentage: 80
    asserts:
      - contains:
          path: spec.metrics
          content:
            type: Resource
            resource:
              name: cpu
              target:
                type: Utilization
                averageUtilization: 80

  - it: should not include CPU metric when targetCPUUtilizationPercentage is not set
    set:
      db.rest.autoscaling.enabled: true
      db.rest.autoscaling.targetCPUUtilizationPercentage: null
      db.rest.autoscaling.targetMemoryUtilizationPercentage: 70
    asserts:
      - notContains:
          path: spec.metrics
          content:
            type: Resource
            resource:
              name: cpu
          any: true

  # ============================================================================
  # Memory metric tests
  # ============================================================================
  - it: should include memory metric when targetMemoryUtilizationPercentage is set
    set:
      db.rest.autoscaling.enabled: true
      db.rest.autoscaling.targetMemoryUtilizationPercentage: 70
    asserts:
      - contains:
          path: spec.metrics
          content:
            type: Resource
            resource:
              name: memory
              target:
                type: Utilization
                averageUtilization: 70

  - it: should not include memory metric when targetMemoryUtilizationPercentage is 0
    set:
      db.rest.autoscaling.enabled: true
      db.rest.autoscaling.targetCPUUtilizationPercentage: 80
      db.rest.autoscaling.targetMemoryUtilizationPercentage: 0
    asserts:
      - notContains:
          path: spec.metrics
          content:
            type: Resource
            resource:
              name: memory
          any: true

  # ============================================================================
  # Labels tests
  # ============================================================================
  - it: should include standard labels
    set:
      db.rest.autoscaling.enabled: true
    asserts:
      - exists:
          path: metadata.labels["helm.sh/chart"]
      - exists:
          path: metadata.labels["app.kubernetes.io/name"]
      - exists:
          path: metadata.labels["app.kubernetes.io/instance"]
