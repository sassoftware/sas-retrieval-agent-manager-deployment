apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "storage.fullname" . }}-pdb
  namespace: {{ .Release.Namespace }}
spec:
  minAvailable: 10%
  selector:
    matchLabels:
      app.kubernetes.io/name: filebrowser
