{{- $dot := . -}}
{{- $globalEnabled := false -}}
{{- if hasKey .Values "global" -}}
{{- if hasKey .Values.global "ingress" -}}
{{- if .Values.global.ingress.enabled -}}
{{- $globalEnabled = .Values.global.ingress.enabled -}}
{{- end -}}
{{- end -}}
{{- end -}}
{{- if or (index .Values "sas-retrieval-agent-manager-api").ingress.enabled $globalEnabled -}}
{{- $fullName := include "sas-retrieval-agent-manager-api.fullname" . | lower -}}
{{- $svcPort := (index .Values "sas-retrieval-agent-manager-api").service.port -}}
{{- if and (index .Values "sas-retrieval-agent-manager-api").ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey (index .Values "sas-retrieval-agent-manager-api").ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set (index .Values "sas-retrieval-agent-manager-api").ingress.annotations "kubernetes.io/ingress.class" (index .Values "sas-retrieval-agent-manager-api").ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "sas-retrieval-agent-manager-api.labels" . | nindent 4 }}
  {{- with (index .Values "sas-retrieval-agent-manager-api").ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
{{- if $globalEnabled | default false }}
  {{- if and (.Values.global.ingress.className | default (index .Values "sas-retrieval-agent-manager-api").ingress.className) (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.global.ingress.className | default (index .Values "sas-retrieval-agent-manager-api").ingress.className  }}
  {{- end }}
  {{- if and ((include "testValuesPath" (list .Values "global" "ingress" "tls" "enabled")) | eq "true"  | default false) (.Values.global.ingress.tls.enabled | default false) }}
  tls:
    - hosts:
      - {{ .Values.global.domain | quote }}
      {{- if (include "testValuesPath" (list .Values "ingress" "tls" "hosts")) | eq "true" }}
        {{- range (index .Values "sas-retrieval-agent-manager-api").ingress.tls.hosts }}
          {{- if and . (ne . "") }}
      - {{ . | quote }}
          {{- end }}
        {{- end }}
      {{- end }}
      {{- if ((include "testValuesPath" (list .Values "global" "ingress" "tls" "secretName")) | eq "true") }}
      secretName: {{ .Values.global.ingress.tls.secretName | default "" }}
      {{- else if ((include "testValuesPath" (list .Values "ingress" "tls" "secretName")) | eq "true") }}
      secretName: {{ (index .Values "sas-retrieval-agent-manager-api").ingress.tls.secretName | default "" }}
      {{- end }}
  {{- end }}
  rules:
    - host: {{ .Values.global.domain | quote }}
      http:
        paths:
          {{- range (index .Values "sas-retrieval-agent-manager-api").ingress.paths }}
          - path: {{ .path}}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
  {{- else }}
  {{- if and (index .Values "sas-retrieval-agent-manager-api").ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ (index .Values "sas-retrieval-agent-manager-api").ingress.className }}
  {{- end }}
  {{- if (index .Values "sas-retrieval-agent-manager-api").ingress.tls }}
  tls:
    {{- range (index .Values "sas-retrieval-agent-manager-api").ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range (index .Values "sas-retrieval-agent-manager-api").ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
          {{- if $.Values.global.configuration.swagger }}
          - path: /openapi.json
            {{- if (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: ImplementationSpecific
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
  {{- end }}
{{- end }}