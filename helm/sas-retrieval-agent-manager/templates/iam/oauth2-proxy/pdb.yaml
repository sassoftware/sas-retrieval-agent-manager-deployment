apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "keycloak.oauthProxy.fullname" . }}-pdb
  namespace: {{ .Release.Namespace }}
spec:
  minAvailable: 10%
  selector:
    matchLabels:
      app.kubernetes.io/name: oauth2-proxy
