{{- if .Values.iam.keycloak.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "keycloak.fullname" . }}
  labels:
    {{- include "keycloak.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "keycloak.fullname" . }}
  minReplicas: {{ .Values.iam.keycloak.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.iam.keycloak.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.iam.keycloak.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.iam.keycloak.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.iam.keycloak.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.iam.keycloak.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
