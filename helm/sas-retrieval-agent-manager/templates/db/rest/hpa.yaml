{{- if .Values.db.rest.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "postgrest.fullname" . }}
  labels:
    {{- include "postgrest.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "postgrest.fullname" . }}
  minReplicas: {{ .Values.db.rest.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.db.rest.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.db.rest.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.db.rest.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.db.rest.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.db.rest.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
