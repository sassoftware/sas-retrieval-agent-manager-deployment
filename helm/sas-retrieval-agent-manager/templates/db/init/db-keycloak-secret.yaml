apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-keycloak-db-credentials" (include "retrieval-agent-manager.fullname" .) }}
  labels:
    {{- include "db-initialization.labels" . | nindent 4 }}
stringData:
  user: {{ .Values.users.keycloak.user.username | quote }}
  password: {{ .Values.users.keycloak.user.password | quote }}
