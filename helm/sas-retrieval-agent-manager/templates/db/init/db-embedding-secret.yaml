apiVersion: v1
kind: Secret
metadata:
  name: embedding-db-credentials
  labels:
    {{- include "sas-retrieval-agent-manager-db-init.labels" . | nindent 4 }}
stringData:
  user: {{ .Values.global.configuration.embedding.user | quote }}
  password: {{ .Values.global.configuration.embedding.password | quote }}
