apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-embedding-db-credentials" (include "retrieval-agent-manager.fullname" .) }}
  labels:
    {{- include "db-initialization.labels" . | nindent 4 }}
stringData:
  user: {{ .Values.users.embedding.username | quote }}
  password: {{ .Values.users.embedding.password | quote }}
