{{- if .Values.db.init.config.vectorStore.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-vector-job-db-credentials" (include "retrieval-agent-manager.fullname" .) }}
  labels:
    {{- include "db-initialization.labels" . | nindent 4 }}
stringData:
  user: {{ .Values.users.vectorizationJob.username | quote }}
  password: {{ .Values.users.vectorizationJob.password | quote }}
{{- end }}
