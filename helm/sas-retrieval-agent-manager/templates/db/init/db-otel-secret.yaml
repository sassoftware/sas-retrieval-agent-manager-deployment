apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-otel-db-credentials" (include "retrieval-agent-manager.fullname" .) }}
  labels:
    {{- include "db-initialization.labels" . | nindent 4 }}
stringData:
  user: {{ .Values.users.otel.username | quote }}
  password: {{ .Values.users.otel.password | quote }}
