{{- if .Values.db.migration.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "db-migration.serviceAccountName" . }}
  labels:
    {{- include "db-migration.labels" . | nindent 4 }}
  {{- with .Values.db.migration.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.db.migration.serviceAccount.automount }}
{{- end }}
