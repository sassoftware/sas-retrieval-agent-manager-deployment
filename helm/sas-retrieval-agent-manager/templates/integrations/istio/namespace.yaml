{{- /* Namespace creation is intentionally omitted from this chart.
     The release namespace must be created before install, either:
       - via the --create-namespace flag on `helm upgrade --install`, or
       - by pre-creating the namespace/project manually.

     When integrations.istio.enabled=true, apply the waypoint label manually
     after the namespace exists:
       kubectl label namespace {{ .Release.Namespace }} \
         istio.io/use-waypoint={{ .Values.integrations.istio.waypoint.name | default "waypoint" }}
*/ -}}
