apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: linkerd-policy-validator
  namespace: {{ .Release.Namespace }}
spec:
  secretName: linkerd-policy-validator-k8s-tls
  secretTemplate:
    labels:
      app.kubernetes.io/part-of: Linkerd
  duration: 168h    # 7 days duration.
  renewBefore: 24h  # Renew 1 day before expiration.
  issuerRef:
    name: linkerd-webhook-issuer
    kind: ClusterIssuer
  commonName: linkerd-policy-validator.{{ .Release.Namespace }}.svc
  dnsNames:
    - linkerd-policy-validator.{{ .Release.Namespace }}.svc
    - linkerd-policy-validator.{{ .Release.Namespace }}.svc.cluster.local
  isCA: false
  usages:
    - server auth